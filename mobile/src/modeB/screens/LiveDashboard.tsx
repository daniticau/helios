// TODO(ws4): Live dashboard — current action, $/hr, battery SoC, forecast.
import { Text, View } from 'react-native';

export function LiveDashboard() {
  return (
    <View>
      <Text>LiveDashboard — WS4 stub</Text>
    </View>
  );
}
