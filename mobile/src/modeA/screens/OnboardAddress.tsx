// TODO(ws3): Mode A — address entry screen.
// Acceptance: geocode entry → populates profile.lat/lng.
import { Text, View } from 'react-native';

export function OnboardAddress() {
  return (
    <View>
      <Text>OnboardAddress — WS3 stub</Text>
    </View>
  );
}
