// TODO(ws3): Mode A — utility + bill entry.
import { Text, View } from 'react-native';

export function OnboardUtility() {
  return (
    <View>
      <Text>OnboardUtility — WS3 stub</Text>
    </View>
  );
}
