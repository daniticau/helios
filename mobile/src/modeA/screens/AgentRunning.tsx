// TODO(ws3): THE hero screen. Live Orthogonal ticker as calls resolve.
// This is the demo moment — see HELIOS.md §11 and §17.
import { Text, View } from 'react-native';

export function AgentRunning() {
  return (
    <View>
      <Text>AgentRunning — WS3 stub (polish this beyond everything else)</Text>
    </View>
  );
}
